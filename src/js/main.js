$(document).ready(function(){
  $('.slick-gallery').slick({
    dots: false,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    prevArrow: '#prev-slide',
    nextArrow: '#next-slide'
  })
});
